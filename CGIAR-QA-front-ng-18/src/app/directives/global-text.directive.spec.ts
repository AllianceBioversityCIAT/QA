import { GlobalTextDirective } from './global-text.directive';

describe('GlobalTextDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobalTextDirective();
    expect(directive).toBeTruthy();
  });
});
